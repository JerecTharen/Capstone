import { UnknownImgPipe } from './unknown-img.pipe';

describe('UnknownImgPipe', () => {
  it('create an instance', () => {
    const pipe = new UnknownImgPipe();
    expect(pipe).toBeTruthy();
  });
});
